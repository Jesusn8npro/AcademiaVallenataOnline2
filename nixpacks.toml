# 🚀 Configuración Nixpacks optimizada para SvelteKit
# Academia Vallenata Online - Solución definitiva

[variables]
NODE_VERSION = "18"

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmd = "npm ci --only=production=false"

[phases.build]
cmd = "npm run build"

[start]
cmd = "node build" 