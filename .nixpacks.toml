[phases.setup]
nixPkgs = ['nodejs_18', 'nodePackages.npm']
aptPkgs = []

[phases.install]
cmds = ['npm ci --omit=dev']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm start'

[variables]
NODE_ENV = 'production' 